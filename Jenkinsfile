pipeline {
    agent any
    stages {
    stage('code checkout') {
        steps {
            checkout scm
        }
    }
        stage('build') {
            steps {
                sh 'date'
            }
        }
    }
}
